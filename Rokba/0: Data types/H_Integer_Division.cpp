#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main(){

    long double x;
    cin >> x; 

    x = floor(x/10);

    cout << setprecision(0) << fixed << endl;
    cout << x << endl;
}