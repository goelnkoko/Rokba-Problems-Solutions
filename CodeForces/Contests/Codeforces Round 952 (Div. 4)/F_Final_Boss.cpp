#include<bits/stdc++.h>
#define fast ios_base::sync_with_stdio(false), cin.tie(NULL), cout.tie(NULL)
#define endl '\n'
#define ll long long

using namespace std;

int main(){
    fast;

    int t;
    cin >> t;

    while(t--){

        ll h, n;
        cin >> h >> n;

        vector<int> a(n), c(n);

        for(int i=0; i < n; i++)
            cin >> a[i];

        for(int j=0; j < n; j++)
            cin >> c[j];

        
    }

  return 0;
}